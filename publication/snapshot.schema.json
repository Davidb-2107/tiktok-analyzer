{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$defs": {
    "runtime": {
      "type": "object",
      "required": [
        "taxonomy",
        "channels",
        "formulas",
        "cards",
        "mappings",
        "resolved_compilation_inputs"
      ],
      "properties": {
        "resolved_compilation_inputs": {
          "$ref": "#/$defs/resolved_compilation_inputs"
        }
      }
    },
    "resolved_compilation_inputs": {
      "type": "object",
      "required": ["target_wpm", "target_duration_s", "shot_duration_s"],
      "properties": {
        "target_wpm": {"type": "string"},
        "target_duration_s": {"type": "array", "items": {"type": "string"}},
        "shot_duration_s": {"type": "array", "items": {"type": "string"}}
      }
    },
    "payload": {
      "type": "object",
      "required": ["runtime"],
      "properties": {
        "runtime": {"$ref": "#/$defs/runtime"}
      },
      "additionalProperties": false
    },
    "provenance": {
      "type": "object",
      "required": [
        "vault_commit",
        "builder_version",
        "taxonomy_module_digest",
        "module_digests",
        "sot_versions",
        "voice_profile_digest",
        "identity_history",
        "build_freshness"
      ]
    },
    "media_digest": {
      "type": "object",
      "required": ["media_id", "extension", "sha256", "size"],
      "properties": {
        "media_id": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]{0,127}$"},
        "extension": {"type": "string", "enum": [".jpg", ".jpeg", ".png", ".webp"]},
        "sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
        "size": {"type": "integer", "exclusiveMinimum": 0}
      },
      "additionalProperties": false
    }
  },
  "title": "Snapshot manifest v1",
  "type": "object",
  "required": [
    "schema_version",
    "canonicalization_version",
    "hash_algorithm",
    "project_id",
    "project_id_scheme",
    "release_id",
    "payload_digest",
    "provenance"
  ],
  "properties": {
    "schema_version": {"const": 1},
    "canonicalization_version": {"const": "json-c14n-v1"},
    "hash_algorithm": {"const": "sha256"},
    "project_id": {"type": "string"},
    "project_id_scheme": {"type": "string"},
    "release_id": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "payload_digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "provenance": {"$ref": "#/$defs/provenance"},
    "media_digests": {
      "type": "array",
      "items": {"$ref": "#/$defs/media_digest"}
    }
  },
  "additionalProperties": false
}
